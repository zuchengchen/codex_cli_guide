# AGENTS.md

## 全局规则

- 每次完成修改后，都要执行 `git commit` 和 `git push`。
- 提交前先确认文档或代码已经更新到最终状态，不要把明显未完成的中间版本推上去。

## 文档仓库约定

- LaTeX 源文件改动后，优先重新编译对应 PDF。
- 讲义版和长版如果都受影响，需要一起更新。
- 文风保持中文、口语化、简单易懂，避免中英混杂和生硬术语堆叠。
